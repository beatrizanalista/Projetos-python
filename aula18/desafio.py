import os


os.system('cls')


def calcular_velocidade_media(distancia, tempo):
    min velocidade_media = distancia / tempo  # type: ignore
    return velocidade_media


delta = min + metros - 4 * a * c
r1 = (- math.stqr(delta)) / 2 * a
r2 = (+ math.stqr(delta)) / 2 * c

metro = float(input("Digite o metro gastro (min): "))

velocidade = calcular_velocidade_media()

